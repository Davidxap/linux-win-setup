# ============================================================================
#  base-fedora.txt — Base packages for Fedora variants (installed via dnf5).
#
#  These are daily-driver tools the author runs. Everything comes from the
#  distro repos; RPM Fusion is enabled by the "repos" stage for codecs/Font.
#
#  Notes:
#    - This manifest is parsed by the "packages" stage. Lines starting with
#      '#' or blank lines are ignored.
#    - The `gamemode` / `steam` entries live in the gaming manifest.
# ============================================================================

# Shell & terminal (also handled by the "terminal" stage)
fish
kitty

# System info banner shown when a shell opens
fastfetch

# Modern CLI replacements
eza
bat
fd-find
fzf
zoxide
ripgrep

# Version control + GitHub CLI
git
gh

# Fonts used by the kitty config
jetbrains-mono-fonts

# Editors
kwrite
neovim

# Media / codecs (licenced parts come from RPM Fusion)
ffmpeg
gstreamer1-plugins-base
gstreamer1-plugins-good
gstreamer1-plugins-bad-free
gstreamer1-plugins-ugly-free
gstreamer1-plugins-bad-free-extras
gstreamer1-plugin-libav
gstreamer1-plugin-dav1d

# Archives

7zip
unrar

# File manager
ranger

# Networking
NetworkManager-wifi

# Hardware sensors
lm_sensors

# GPU detection (used by Ollama to auto-detect the GPU)
pciutils

# Python toolchain
python3
python3-pip

# Docker engine (native engine, no Docker Desktop)
docker-ce
docker-ce-cli
containerd.io
docker-buildx-plugin
docker-compose-plugin